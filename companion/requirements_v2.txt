# PTSL Integration Requirements (v2 - using py-ptsl)
# =====================================================

# Core gRPC/Protobuf (required by py-ptsl)
grpcio>=1.76.0
protobuf>=6.33.0

# Audio format conversion (FLAC → WAV)
soundfile>=0.12.1

# py-ptsl library (professional PTSL wrapper)
# Install from local repository as editable package
# Run: pip install -e external/py-ptsl
